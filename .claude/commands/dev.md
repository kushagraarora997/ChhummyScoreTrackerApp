---
description: Start the Vite dev server for Chhummy Tracker
---

Run the development server:

```bash
npm run dev
```

The app runs at http://localhost:5173. Open on phone via local IP (check `ipconfig` for your machine's IP on the same WiFi, e.g. http://192.168.x.x:5173).
